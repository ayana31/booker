<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>
    <h2> Title</h2>
    <%= f.text_field :title, class:"book_title" %>
    <h2> Body</h2>
    <%= f.text_area :body, class:"book_body" %>

    <br><br><%= f.submit 'Update Book' %>
    <br><br>
    <%= link_to"Show", book_path(@book), class:"show_#{@book.id}" %>
    |
    <%= link_to"Back", books_path, class:"back" %>

<% end %>